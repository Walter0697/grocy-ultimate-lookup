# Roadmap

## Product Edit History

- current scope: add server-side search, entry detail diff view, and barcode
  audit summary for dashboard product edit history
- next scope: detect and surface cases where one barcode has been linked to
  multiple product ids over time instead of assuming the latest product
  snapshot is sufficient
